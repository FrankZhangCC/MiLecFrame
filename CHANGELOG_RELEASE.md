# 更新历史（发行版）

> 本文件仅收录正式发行版的重点功能新增。详细的开发过程变更记录见 [CHANGELOG.md](./CHANGELOG.md)。
>
> **号段说明（2026-09-25）**：版本体系已切换为 dev / release 两线模型，历史发行条目（v2.5.2 及更早）随旧号段一并作废，本文件从 v2.6.0 起重新累积。

---

## v2.6.0 (2026-09-25)

> 矩形描边/填充/高斯模糊效果栈 + 全图高斯两级模糊缓存 + 旋转适配（用户可选顺/逆时针整帧旋转）。

### 矩形三层效果栈

- 每个自定义矩形可独立开启纯色填充、高斯模糊磨砂底、内描边，固定合成层级"模糊 → 填充 → 内描边"，内描边不改变矩形外部尺寸与定位
- 未使用新字段的全部内置样式输出像素级不变（18 张基线矩阵回归恒一致），平滑升级
- 描边色支持明暗自适应（按背景亮度自动取色），厚度按原图短边比例自适应换算
- 照片内矩形按原图坐标精确取样模糊；完全覆盖场景自动跳过模糊计算
- D2 零配置升级：FrameBar 类样式只需追加效果字段即可升级磨砂信息条

### 性能：全图高斯统一短路 + 两级模糊缓存

- 高斯背景常见场景不再全量模糊（33MP 样片 2.64s → 0.10s，约 26 倍）
- 一级缓存（每帧）：背景与多个矩形共享同源卷积，多矩形零重复计算
- 二级缓存（跨帧，GUI 交互）：切换背景颜色/透明度/饱和度即时生效不再重新卷积

### 旋转适配

- 用户可显式选择顺/逆时针整帧旋转，对横图/竖图/方形图均生效；样式预设可声明竖图默认方向
- 图片处理页新增"旋转适配"菜单（默认跟随样式/不旋转/顺时针/逆时针），动态提示
- 未声明样式输出与历史版本逐像素一致

### 修复

- 修复信息栏格式显示"未知"的问题（EXIF 转正副本属性回填）
