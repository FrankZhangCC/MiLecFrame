# 更新历史（发行版）

> 本文件仅收录正式发行版的重点功能新增。详细的开发过程变更记录见 [CHANGELOG.md](./CHANGELOG.md)。

---

## v2.5.2 (2026-09-12)

> 竖拍照片方向修复：竖拍照片全程正确显示与输出，竖图自动启用短版镜头名。
> 本版本包含 mainline v2.5.0-dev 以来的累积更新（期间无公开发行）。

### 竖拍照片方向修复

- 修复佳能等相机竖拍照片导入后变横向的问题：处理链路按 EXIF Orientation 标签自动转正像素，输出与拍摄方向一致（HDR/HEIF/AVIF 同样支持）
- 输出文件同步重置方向标记，看图软件不会二次旋转；机型、镜头、拍摄时间等 EXIF 信息完整保留
- 导入缩略图、文件尺寸信息与大图预览同步修正方向
- 竖拍照片导入时自动启用"短版镜头名"显示（延续原 Streamlit 版设计）

### 其他修复

- 修复未显式指定背景填充类型时因默认参数非法导致处理报错的问题

## v1.2.0 (2026-08-14)

> 便携版打包发行、样式编辑器、拍摄时间显示控制、矩形装饰元素。

### 便携版打包发行

- 提供 Windows 免安装便携版（onedir 文件夹 + zip 发行包），拷贝即用
- 可写数据（用户配置、设备映射库、日志、自建样式）保存在 exe 同目录，升级不丢失
- 应用图标支持多尺寸 ICO，发行字体按需精简（发行包体积约 100MB）
- 修复命令行（CLI）单张/批量处理模式的导入崩溃问题

### 样式编辑器

- GUI 内可视化新建/编辑相框样式，实时预览渲染效果
- 样式以文件夹变体组织（default.yaml / no_location.yaml 等），按数据可用性自动匹配

### 拍摄时间显示模式控制

- GUI 与 CLI 均支持拍摄时间显示模式：`full`（完整日期时刻）/ `date_only`（仅日期）/ `hide`（隐藏）
- 隐藏时自动匹配 `no_timestamp.yaml` 样式变体

### 装饰元素与细节

- 新增自定义矩形（rectangles）装饰，支持半透明装饰色块
- EXIF 信息栏焦距改用 35mm 等效焦距显示
- LOGO 品牌尺寸补偿系数外置为可编辑 YAML 配置文件

### 修复（v1.2.0 增补）

- 修复批量导入多张相同相机/镜头的图片时，设备映射库重复写入的问题
  （此前同一设备会在设备管理列表末尾出现多条相同记录）

---

## v1.1.0-release (2026-06-19)

样式选择器升级为缩略图滚动浏览，样式配置支持自定义背景色，GUI 适配深色模式。

### 样式选择器重构

- 相框样式从文字下拉列表改为**横向缩略图滚动条**，直观预览每种样式的效果
- 每种样式放置一张 512×512 预览图（`thumbnail.png`）即可在样式选择器中显示
- 未放置缩略图的样式以灰色占位文字展示，不影响使用
- 支持滚轮横向浏览，点击缩略图切换样式

### 样式配置自定义背景色

- 样式 YAML 新增 `custom_bg_color` 字段，可指定任意十六进制颜色（如 `"#28180B"`）或 RGB 数组
- 可选声明 `custom_bg_text_scheme`（`dark`/`light`）控制文字颜色，未声明时自动根据背景亮度判定
- 指定自定义背景色后，GUI 的背景填充下拉框自动禁用，保证样式的完整性

### 色彩管理与深色模式适配

- 修复广色域显示器（如 Display P3）上图片过饱和的问题，Qt 6 色域转换确保色彩准确

### 交互改进

- **拖放加载图片**：直接将图片文件拖入页面即可加载到胶片栏，无需点击添加按钮
- **Delete 键移除**：选中胶片栏图片后按 Delete 键快速移除（文本输入框中不会误触发）

### 样式配置文件夹组织

- 所有样式配置文件迁移至独立文件夹，方便存放缩略图和附属资源

---

## v1.0.0-release (2026-06-09)

首个正式版，带来全新的原生桌面体验。

### 全新桌面界面

界面从网页版全面迁移至原生桌面应用，采用现代化设计语言，操作更流畅、功能更集中。

- **主窗口导航**：左侧导航栏清晰组织所有功能页面，一键切换
- **图像处理页**：左侧大图预览区 + 右侧参数配置面板，布局宽敞
  - 底部胶片栏：多张图片横向排列，已处理和未处理状态一目了然，支持滚轮浏览
  - 图片信息面板：相机型号、镜头、焦距、光圈、快门、ISO 等关键数据自动提取并展示
  - 配置面板分为五个区：输出设置、相框样式、个性化信息、拍摄信息、水印，折叠式设计让界面更清爽
- **相机映射管理**：可按品牌筛选，表格内直接编辑相机型号映射
- **镜头映射管理**：支持搜索过滤，轻松管理镜头名称映射
- **样式编辑器**：所见即所得的相框样式设计工具
  - 十个配置模块分页导航，从画布尺寸到字体颜色均可自定义
  - 右侧实时预览，修改即时生效，支持横竖样本图切换
  - 一键导出完整样式配置

### Logo 显示优化

- 不同品牌的 Logo 可独立控制缩放比例，哈苏、徕卡等细长 Logo 显示更协调
- Logo 尺寸上限可自由调节，适应各种形状的品牌标识

### 其他改进

- 网页版界面已封存，旧版不再维护
- 程序启动更简单：双击即可打开，无需记忆任何命令
- 帮助文档全面更新，操作指引更清晰

---

## v0.1.0-release (2026-05-25)

首个发行预览版，带来丰富的相框自定义能力。

### 背景填充

全新的背景系统，支持多种填充效果：

- **纯色背景**：简洁的黑色或白色，适合干净利落的展示风格
- **艺术模糊**：四种透明度可选的模糊效果，将原图色彩延伸至相框边缘，过渡自然
- **智能文字配色**：根据背景深浅自动选择深色或浅色文字，确保始终清晰可读

### 灵活的布局定位

相框内的文字和装饰元素可以自由摆放：

- **固定位置**：将元素固定在相框的任意边缘或角落，上下左右随心放置
- **相对排列**：元素之间可以相互参照排列，例如将拍摄日期紧跟在相机型号下方
- **组合居中**：多个元素组成一个整体后整体居中，排版更整齐
- **安全边距**：自动防止文字溢出相框边界，确保每一张出片都完整
- **智能保护**：当引用的信息缺失时自动调整位置，不会出现错位或重叠

### 样式变体系统

同一款相框样式可以根据照片信息自动适配不同的排版：

- 照片有拍摄地点时完整展示，无地点时自动切换为简洁版布局
- 即使作者名缺失也能保持排版美观
- 只需设计一次，即可覆盖所有使用场景

### 丰富的文字内容

除了自动提取的拍摄参数，还支持更多文字展示方式：

- **预设标签**：可以在相框中展示固定的文字标签，如镜头焦距、光圈值等
- **自定义寄语**：输入你想说的话，支持多行文字，让每张照片都有专属印记
- **独立参数展示**：焦距、光圈、快门、ISO 可以分开独立排版，不再挤在一行

### 字体系统升级

- 英文和中文使用各自独立的字体，西文更优雅、中文更清晰
- 即使没有安装自定义字体，系统也会自动使用 Windows 内置字体，开箱即用
- 支持细体、常规、中等三种字重，可根据画面风格自由选择
- 多行文字可调整行间距，排版更透气

### 照片增强处理

- **圆角裁切**：可以为照片的四角设置独立的圆角弧度，模拟胶片或拍立得质感
- **色彩精准**：自动识别并转换 Adobe RGB、ProPhoto RGB 等专业色彩空间，颜色显示准确无误
- **HDR 支持**：手机拍摄的 HEIC、AVIF 等高动态范围照片自动色调映射，保留丰富细节
- **背景增强**：高斯模糊背景自动提升色彩饱和度，解决模糊后颜色变淡的问题
- **EXIF 信息保留**：处理后的照片依然保留原始拍摄数据和色彩配置文件

### 相机与镜头管理

- 预置主流相机品牌（佳能、尼康、索尼、富士、哈苏等）的型号映射库
- 镜头名称可设置简短版，适合在竖版照片中节省空间
- 三种镜头显示模式：相机+镜头、仅显示相机、仅显示镜头
- 设备映射表可在界面中直接编辑修改

### GPS 与 Logo

- **拍摄地点**：支持从照片 EXIF 中提取 GPS 坐标，自动转换为度分秒格式显示
- **品牌 Logo**：根据相机品牌自动匹配对应 Logo 并展示在相框中，支持浅色和深色背景自动选择合适颜色

### 批量处理

- 支持批量处理整个文件夹的照片，统一应用相同样式和参数
- 每张照片独立识别相机品牌并匹配合适的 Logo
- 可跳过已处理的文件，重复运行也不重复生成
- 实时进度显示，大批量处理心中有数

### 更多工具改进

- 调试日志自动滚动保留最近 1000 行，排查问题更方便
- 乱码兼容处理：自动适配多种文字编码，EXIF 中的特殊字符也能正常显示

---

> 详细开发变更记录见 [CHANGELOG.md](./CHANGELOG.md)。
